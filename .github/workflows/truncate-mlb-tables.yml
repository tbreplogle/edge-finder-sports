- name: Call snapshot_and_truncate()
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
  run: |
    set -euo pipefail
    psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "call public.snapshot_and_truncate();"
