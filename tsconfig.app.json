{
  "extends": "./tsconfig.json",      // <- inherit base config (incl. alias)
  "compilerOptions": {
    "types": ["vite/client"]         // app-specific extras (optional)
  },
  "include": ["src"]
}
